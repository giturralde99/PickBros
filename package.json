{
  "name": "pickbros",
  "version": "1.0.0",
  "description": "PickBros - Copa del Mundo 2026",
  "private": true,
  "scripts": {
    "build": "echo 'No build step required for static HTML project'",
    "lint": "echo 'No linter configured'",
    "test": "echo 'No tests configured'"
  },
  "dependencies": {
    "@vercel/speed-insights": "^1.0.0"
  }
}
